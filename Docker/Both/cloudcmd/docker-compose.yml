version: '3.9'
services:
  cloudcmd:
    image: coderaiser/cloudcmd:17.0.3
    container_name: cloudcmd
    hostname: cloudcmd
    ports:
      - 2861:8000
    volumes:
      - ~:/root
      - /:/mnt/fs
    networks:
      - proxy

networks:
  proxy:
    driver: bridge
    external: true
